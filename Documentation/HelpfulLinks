LDAP text file formatting and info about adding user

    https://www.thegeekstuff.com/2015/02/openldap-add-users-groups

    another good source for this info*****

    https://www.learnitguide.net/2017/11/how-to-create-ldap-users-and-groups.html

LDAP group schema

    ####Taken from ldap export in LAM

    # Search scope: base
    # Search filter: (objectClass=*)
    # Total entries: 1
    #
    # Generated by LDAP Account Manager (http://www.ldap-account-manager.org) on December 8, 2018 4:22 am
    # Version: 6.2

    version: 1

    # Entry 1: cn=mullermm,ou=groups,dc=babbage,dc=augsburg,dc=edu
    dn: cn=mullermm,ou=groups,dc=babbage,dc=augsburg,dc=edu
    cn: mullermm
    gidnumber: 1002
    objectclass: posixGroup

LDAP user schema

    Command : ldapsearch -h 141.224.38.247 -x -s base -b "uid=mullermm,ou=users,dc=babbage,dc=augsburg,dc=edu"

    # mullermm, users, babbage.augsburg.edu
    dn: uid=mullermm,ou=users,dc=babbage,dc=augsburg,dc=edu
    objectClass: posixAccount
    objectClass: inetOrgPerson
    objectClass: organizationalPerson
    objectClass: person
    loginShell: /bin/bash
    uid: mullermm
    cn: mullermm
    gecos: Matthew Muller
    uidNumber: 1002
    gidNumber: 1002
    sn: Muller
    givenName: Matthew
    homeDirectory: /nfs/home/mullermm




ldapadd -x -W -D "cn=admin,dc=babbage,dc=augsburg,dc=edu" -f ldapUserEntry.ldif

ldappasswd -s welcome123 -W -D "cn=ramesh,dc=tgs,dc=com" -x "uid=delgadoa,ou=users,dc=tgs,dc=com"

ldapadd -x -W -D "cn=admin,dc=babbage,dc=augsburg,dc=edu" -f ldapGroupEntry.ldif

ldapmodify -x -W -D "cn=admin,dc=babbage,dc=augsburg,dc=edu" -f ldapAddToGroup.ldif



















